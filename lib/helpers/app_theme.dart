import 'package:flutter/material.dart';

class MyAppThemeProperties {
  static const actionColor = Colors.deepPurple;
  static final backGroundColor = Colors.purple[50];
  static const actionIconTheme = IconThemeData(color: Colors.deepPurple);
  static const appBarIconTheme = IconThemeData(color: Colors.deepPurple);
  static const titleTextColor = TextStyle(color: Colors.black, fontSize: 25);
}
